项目环境搭建：
（1）默认opencv环境已经搭建成功。
（2）在Visual Studio新建一个空项目，配置属性管理器，将活动解决方案平台设置成x64,然后在Debug|x64文件夹下导入opencv_64.props文件。
（3）在工程文件中main.cpp所在的文件夹下新建两个文件夹（result/和sample/）。
（4）将需要处理的图片资源放到sample/文件夹下。
（5）将main.cpp中复制到工程中的源文件夹下。
（6）根据实际情况分别修改s_format,n和m的值。
